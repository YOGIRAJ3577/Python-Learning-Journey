#type casting and type conversion

print (1+1.2) #this will give 2.2 because python automatically converts int to float it called as implicit type conversion

print (int(1.2)+1) #this will give 2 because we are explicitly converting float to int it called as explicit type conversion


# automatic conversion by python interpriter is called implicit type conversion and manual conversion by user is called explicit type conversion